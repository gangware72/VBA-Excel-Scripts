Sub CopyTo9Box()    'Loop variables    Dim i As Integer 'This is going to be the loop counter    Dim lastRow As Integer 'Last row    Dim currentCell As String            'Declaring the placeholders for each othe 9 box squares    Dim one, two, three, four, five, six, seven, eight, nine As Integer        'Setting the values for each box square    one = 37: two = 37: three = 37: four = 21: five = 21: six = 21: seven = 4: eight = 4: nine = 4        'Select variable for the case    Dim var, iput, box As String        iput = "Employees to Grade"    box = "9 Box"    lastRow = Worksheets(iput).UsedRange.Rows.Count        'lastRow = Sheets("Summary").range("E3").Value 'insert the last row as a manual spot        For i = 5 To lastRow        currentCellScore = "C" & i        currentCellName = "B" & i                var = Sheets(iput).Range(currentCellScore).Value                Select Case var                            Case 1 'the value places here are 16 big. Evenually, the should be 50 max. It starts at C4                Sheets(iput).Range(currentCellName).Copy 'copy name                currentCellName = "C" & one 'the current cell on the Employee chart worksheet                Sheets(box).Range(currentCellName).Select 'select where to paste it                ActiveCell.PasteSpecial (xlPasteValues) 'paste it                one = one + 1 'increment the counter                            Case 2                Sheets(iput).Range(currentCellName).Copy 'copy name                currentCellName = "D" & two 'the current cell on the Employee chart worksheet                Sheets(box).Range(currentCellName).Select 'select where to paste it                ActiveCell.PasteSpecial (xlPasteValues) 'paste it                two = two + 1 'increment the counter                        Case 3                Sheets(iput).Range(currentCellName).Copy 'copy name                currentCellName = "E" & three 'the current cell on the Employee chart worksheet                Sheets(box).Range(currentCellName).Select 'select where to paste it                ActiveCell.PasteSpecial (xlPasteValues) 'paste it                three = three + 1 'increment the counter                        Case 4                Sheets(iput).Range(currentCellName).Copy 'copy name                currentCellName = "C" & four 'the current cell on the Employee chart worksheet                Sheets(box).Range(currentCellName).Select 'select where to paste it                ActiveCell.PasteSpecial (xlPasteValues) 'paste it                four = four + 1 'increment the counter                        Case 5                Sheets(iput).Range(currentCellName).Copy 'copy name                currentCellName = "D" & five 'the current cell on the Employee chart worksheet                Sheets(box).Range(currentCellName).Select 'select where to paste it                ActiveCell.PasteSpecial (xlPasteValues) 'paste it                five = five + 1 'increment the counter                        Case 6                Sheets(iput).Range(currentCellName).Copy 'copy name                currentCellName = "E" & six 'the current cell on the Employee chart worksheet                Sheets(box).Range(currentCellName).Select 'select where to paste it                ActiveCell.PasteSpecial (xlPasteValues) 'paste it                six = six + 1 'increment the counter                        Case 7                Sheets(iput).Range(currentCellName).Copy 'copy name                currentCellName = "C" & seven 'the current cell on the Employee chart worksheet                Sheets(box).Range(currentCellName).Select 'select where to paste it                ActiveCell.PasteSpecial (xlPasteValues) 'paste it                seven = seven + 1 'increment the counter                        Case 8                Sheets(iput).Range(currentCellName).Copy 'copy name                currentCellName = "D" & eight 'the current cell on the Employee chart worksheet                Sheets(box).Range(currentCellName).Select 'select where to paste it                ActiveCell.PasteSpecial (xlPasteValues) 'paste it                eight = eight + 1 'increment the counter                            Case 9                Sheets(iput).Range(currentCellName).Copy 'copy name                currentCellName = "E" & nine 'the current cell on the Employee chart worksheet                Sheets(box).Range(currentCellName).Select 'select where to paste it                ActiveCell.PasteSpecial (xlPasteValues) 'paste it                nine = nine + 1 'increment the counter                            End Select            Next i    End Sub